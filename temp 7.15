import java.util.Scanner;

public class reverseArray {

	public static int[] eliminateDuplicates(int[] list) {
		//note unsure if correct was struggling with this
        for (int i = 0; i < list.length; i++) {
            boolean isDistinct = true;

            for (int h = 0; h < i; h++) {
                if (reader[i] == reader[h]) {
                    isDistinct = false;                    
                }
            }


            if (isDistinct) {
            	distinctArray[i] = reader[i];
            	
                distinct++;
            }
        }
		
	}

	

		
		
    

    public static void main(String[] args) {
    	int distinct = 0;
    	int[] temp = new int[10];

        //10 is the temp number since we wont know the exact yet of distinct
        
    	//used to find distinct
        int[]reader = new int[10];


        Scanner input = new Scanner(System.in);
        System.out.print("Enter 10 integers: ");

        
        for (int i = 0; i < reader.length; i++) {

            reader[i] = input.nextInt();
            
            
        }
        
        
        
        
        int[] distinctArray = eliminateDuplicates(reader);
        
        System.out.println("The number of distinct integers is " + distinct);
        System.out.print("The distinct integers are ");
        
        
        //info print to print out the array thats distinct
        for (int i = 0; i < distinctArray.length; i++) {
        System.out.print(distinctArray[i] + " ");
        }

        }

           
        


}


